# 給接手者（Codex 請先讀這節）

**讀取中文文字檔一律明確指定 UTF-8。**

- PowerShell 執行前先將 `$OutputEncoding` 與 `[Console]::OutputEncoding` 設為 UTF-8，
  不依賴系統預設編碼。
- 若出現亂碼，立即改用 UTF-8 重新讀取；不得依據亂碼內容判斷。

**開始任何工作前，先完整讀取本庫 `CLAUDE.md` 並遵守其中全部指示。**

- `CLAUDE.md` 若要求讀取維護者的全域設定，能存取時照其指示讀取並遵守；
  無法存取時不得臆測內容。
- 專案背景、業務規則、協作偏好、測試、commit、版本／Git／發布等規則，
  一律以 `CLAUDE.md` 為唯一來源；本檔禁止維護重複副本。
