# PoliceRotaSys 技術文件

> 業務規則、協作偏好與已否決的設計在 `AGENTS.md`，本檔不重複，只寫實作。
> 實作與本檔不符時，以本檔為準或回頭修本檔，不要讓兩者分歧。

## §1 架構

```
SQLite（輪番模板 + 月表快照）
    ↓
排班演算法（純函式，零相依）
    ↓
版面模型（純資料：幾列幾欄、每格文字與顏色）
    ↓                    ↓
openpyxl → .xlsx      QPdfWriter → .pdf
```

**版面模型是刻意設計的中間層**：Excel 與 PDF 共用同一份版面定義才會長一樣；
它是純資料，A3 版面才能在無 GUI 環境自動驗證。

### 模組切分

```
main.py                    進入點、組視窗

lib/
  db_schema.py             ★ 結構唯一來源
  db_seed.py               ★ 種子資料唯一來源
  db_utils.py              連線慣例
  db_backup.py             開機損毀檢查、自動備份（GFS）、手動備份、壓縮
  template.py              輪番模板讀寫（群組、槽位）
  plan.py                  ★ 月表：建立、接續、快照、組版（不含 Qt）
  rota.py                  ★ 排班演算法（純函式，零相依）
  layout_model.py          ★ 版面模型（純資料）
  theme.py                 全域樣式公版
  version.py

export/
  xlsx_writer.py           版面模型 → openpyxl
  pdf_writer.py            版面模型 → QPdfWriter

tabs/                      各分頁
ui_utils/                  公版元件（自 PoliceDocSys 搬，見 MIGRATION.md）
tools/                     開發工具，不進 exe
tests/
```

★ 這五支**全部零 Qt 相依**，推算番號與版面計算不必上機就能測。

**相依方向單向**：`tabs/` 可以叫 `lib/`，`lib/` 絕不反過來叫 `tabs/`。
`rota.py` 連 `db_utils` 都不 import，測它不必準備資料庫。

## §2 排班演算法

```
第 i 人在第 d 天的格位 = ((seed_i - 1 + (d - 1)) mod cycle_len) + 1
顯示代碼 = 該格位在快照槽位的 code（is_rest 則一律印「休」並標紅）
```

固定群組（`mode='fixed'`）去掉 `(d - 1)` 那一項，格位永遠不動。

⚠️ **月表快照存 `slot_seq`（第幾格），不存番號**：休沒有番號，存番號就分不出
「1 日是休在第 6 格還是第 7 格」，而第 6 格隔天還是休、第 7 格隔天上 `08`。

## §3 資料庫

分成設定區（隨時可改）與月表區（產出時的快照），**分界是設計重點**。

### 設定區：人員

```sql
Member(member_id, name, active, female, sort_order)
```

- **女警**（`female`）：姓名在 xlsx 與 pdf 印紅色。⚠️ 只有姓名變紅，番號一律黑
- **沒有 `group_member` 表**：誰在哪一組、站哪一格，存在該月快照裡

### 設定區：輪番模板

```sql
Rota_Template(template_id, name, created_at, note)   -- 一份方案，可存多份

T_Group(
  group_id, template_id, name,
  mode,          -- 'rotate' 輪番 / 'fixed' 固定 / 'blank' 空白欄
  range_expr,    -- ★ 範圍式，例 '1-20'、'21-25'、'A-F'、'1-5,8-12'
  header_before, col_weight,
  reverse_order, -- 反向排序（右往左）：月表上這組欄位左右顛倒
  code_position, -- 代號位置 'below'／'above'：只對固定番有意義（種子資料：幹部＝above）
  note, sort_order
)

T_Slot(
  group_id, seq,
  is_rest,       -- 輪休規則
  code_override  -- 需要不照展開結果時才填
)
```

- `range_expr` 展開成 `T_Slot`，`code_override` 逐格蓋掉展開結果
- 循環格數不另存欄位，就是展開後的格數（存了會有兩份真相）
- 模板數量不設上限；要大改又想留原樣，按「複製一份」再改

#### 範圍式規格

```
大輪番   輪番   1-20
固定番   固定   21-25
幹部     固定   A-F
```

- 在群組彈窗按儲存時展開；**只有輪番類型**可設休，固定番展開後一對一綁人
- 支援逗號分段：`1-5,8-12,15`
- 數字自動補零，位數由最大值決定（最大 20 → `01`…`20`；最大 120 → 三位）
- 型態自動判斷：`1-20` 數字、`A-F` 英文、`甲-丁` 天干；英文與天干只支援單一字元
- ⚠️ 改了範圍再儲存，已設好的休與自訂代碼一律清空（`template.py`）
- ⚠️ 代碼衝突比的是**展開後的字面代碼**：`1-20` 展開成 `01`…`20`，`5-8` 展開成 `5`…`8`，兩者不算衝突

##### 範圍式的兩層驗證

| 時機 | 驗什麼 |
|---|---|
| **焦點離開欄位** | 語法：看不看得懂、反向（`20-1`）、自我重疊（`1-10,5-8`）、型態混用（`1-A`）、空白 |
| **按「檢查規則」** | 跨群組：代碼衝突、群組範圍為空、輪番群組全是休 |

代碼衝突要等所有群組都填完才驗得了，所以放第二層。

⚠️ **focus-out 的錯誤提示不得用彈窗**：焦點一離開就跳 modal，使用者會被困在欄位裡
（點別處又跳一次）。用欄位下方紅字；按「檢查規則」時才可以用彈窗。

⚠️ **被覆寫過的格子要有視覺記號**（底色不同），不然日後看到怪代號會以為程式壞了。

### 月表區

```sql
Month_Plan(
  plan_id, year, month,
  origin,      -- '接續上月' / '自訂起始'，只是來源註記
  created_at,
  snapshot     -- ★ 當月規則與姓名順序的完整副本（JSON）
)
```

`snapshot` 裡一個群組：名稱、模式、`header_before`、`col_weight`、`reverse_order`、
`code_position`、註記、展開並套用自訂後的每格代碼與是否為休，以及成員
（**姓名字串、女警與否、1 日的起始格位**，依畫面順序）。
舊快照缺 `reverse_order` 視為不反向。

- **整月結果不落地，只存 1 日那一排**（落地就有兩份真相）
- 代碼存展開後的結果，不存範圍式
- 重印只讀快照，**不重算設定、不回頭讀設定區**。唯一入口 `lib/plan.build_sheet_for`，
  測試 `TestSnapshotIsDetached` 守著

兩顆產生鈕：

| 鈕 | 做什麼 |
|---|---|
| **接續上月** | 沿用上月快照（含規則與名單），從上月最後一天接著推；上月沒月表時提示，不事先反灰 |
| **自訂起始** | 選一份模板，承辦人指定 1 日的起始格位 |

資料庫層不擋修改與刪除；誤改誤刪靠備份救。

### 結構變更

- **開發機**：結構有變就刪掉 `dbfile.db` 重新產生。
- ⚠️ **現場資料庫不能刪**。`create_all` 只會「沒有才建」，不改既有的表，所以
  **新增欄位一律登記到 `db_schema._LATE_COLUMNS`**，開庫時缺欄就 `ALTER TABLE` 補上
  （PITFALLS DB-1）。補欄要給預設值；需要依既有資料推算的（如幹部代號在上方）寫在
  `_add_missing_columns`。這是真實資料的相容退路，不可拿掉。
- 不做通用遷移框架；拿掉欄位、改型別真的遇到再議。
- 加欄位時 `tests/test_db.py` 的 `TestOldDatabaseIsUpgraded` 一起補。

### 容量

一年約 430 列、約 70 KB，不需要為容量做任何妥協。
⚠️ **產出的 xlsx／pdf 不存進資料庫**（十年會累積幾十 MB BLOB，備份變慢）；要重印就重算。

### 備份與損毀檢查（`lib/db_backup.py`）

開機順序（`main.py`）：**損毀檢查 → 補結構與種子 → 自動備份 → 開視窗**。

- **損毀檢查**：每次開機 `PRAGMA quick_check`；每週第一次開機（主備份資料夾本週還沒有週檔）
  多跑 `integrity_check`。明確損毀 → 訊息請聯絡維護人員、從 `backups` 取回備份，**關閉程式**；
  鎖定、忙線等無法判定的情況一律放行。
  ⚠️ 先檢查再補結構：對已損毀的檔做 `ALTER` 會增加搶救難度。
  ⚠️ 先檢查再備份：損毀的檔被輪替進備份，會擠掉還好的舊備份。
- **自動備份（GFS）**：程式旁 `backups/`，日備份留 7 份、週備份留 4 份、月備份留 12 份，
  檔名 `dbfile_backup_day|week|month_YYYYMMDD.db`。有設 `backup_second_dir` 時異地照同規則另備一份。
  用 `sqlite3` backup API 寫 `.tmp` 再原子換名；**任何失敗只寫 error.log、不擋開程式**，
  異地失敗的短句顯示在功能維護分頁。
- **手動備份**：「立即備份」，`dbfile_backup_manual_YYYYMMDD_HHMM.db`，不計入輪替；
  同一分鐘重按先問覆蓋。
- **不做還原**：把備份檔改名 `dbfile.db` 蓋回程式旁。

### 壓縮

功能維護分頁「壓縮資料庫」跑 `VACUUM`，完成後顯示前後大小。

## §4 畫面結構

單視窗多分頁，比照 PoliceDocSys 的操作慣例。

⚠️ **畫面一律程式碼排版，不用 `.ui` 檔**（維護者不開 Designer）。不要為了比照公文系統改回 `.ui`。

```
┌─ 產生月表 ─┬─ 輪番設定 ─┬─ 人員設定 ─┬─ 功能維護 ─┐
```

分頁順序照使用頻率排。

### 共用外觀

- **按鈕純文字**，不加符號或圖示；三種角色——藍 `primary`（一區只放一顆主要動作）、
  灰 `normal`、紅 `danger`，一律 `ui_utils.styleButton`；按鈕列左群編輯內容、右群檢查與存檔。
  唯一的圖示入口是分頁列右上角的說明鈕，走公版 `QPushButton#helpButton`（平常只見圖示）
- **卡片式排版**：分頁底色淺灰，區塊放白色圓角卡片（`ui_utils/card.py` 的 `Card`），標題列右側放按鈕；
  框線中灰 `#c7c7cc` 以上。顏色全部在 `lib/theme.py`
- 確認框統一最小寬度 600

### 程式內使用指引

- 四個主分頁共用 `ui_utils/help_dialog.py` 的 modal 視窗；`main.py` 在所有分頁建立後，
  以 `attachHelpButton` 把說明鈕裝到分頁列右上角。按鈕依 `currentIndex()` 開啟內容，
  固定索引為 0 產生月表、1 輪番設定、2 人員設定、3 功能維護，不以分頁顯示文字猜測。
- 完整內容與 HTML renderer 集中在 `ui_utils/help_content.py`。資料內容不得放色碼；
  HTML 顏色只能引用 `lib/theme.py` 的 `TEXT_COLOR`、`HELP_ACCENT_COLOR`、`HELP_RULE_COLOR`、
  `HELP_MUTED_TEXT_COLOR`、`HELP_WARNING_*`、`HELP_BTN_*` 公開 token。
- ⚠️ **HTML 一律用「表格格子＋`bgcolor`」排版，字級每一處明寫**：`QTextBrowser` 只吃很少的
  CSS——`border-radius`、`flex` 不支援，`<div>` 的邊框與 `margin-bottom` 幾乎不生效，
  `<h1>`／`<h2>` 會套 Qt 自己的標題放大倍率而大到不成比例，`<ol>` 的縮排與行距會散開。
  段落間距用小字級的空段落撐開，條列用兩欄表格做懸掛縮排（寫法沿用 PoliceDocSys 的說明視窗）。
  這一段改版前先看實際畫面，不要換回一般網頁的寫法。
- 說明視窗與說明鈕都不得設定 stylesheet：關閉鈕用 `styleButton(..., "normal")`，
  說明鈕外觀是公版 `QPushButton#helpButton`（平常透明、滑過才浮出灰底），標題下方細橫線
  是 `QLabel#helpRule`，顏色都在 `lib/theme.py`。
  ⚠️ **說明鈕的高度取 `tabBar().sizeHint()`，並包一層容器用外距對齊第一個分頁的上緣**：
  角落元件會被 Qt 垂直置中，而分頁是靠上排的，寫死高度會比分頁低一截、右側還會貼齊
  視窗邊被切到（維護者看截圖指出「歪」的就是這個）。
  說明鈕圖示是 `res/buttons/icon_help.svg`，新增或更名時要同步更新
  `PoliceRotaSys.spec` 的 `datas`，否則 onefile 版會遺失圖示。
- **內文提到按鈕名稱時畫成行內按鈕**（比照公文系統的說明視窗）：以「」框住的名稱若在
  `help_content._BUTTON_ROLES` 表裡，就換成該角色配色的行內色塊。公文系統是把每顆鈕預烤成
  SVG 再嵌圖，本專案改用行內色塊，不必多帶圖檔進 exe，也不必改個按鈕文字就重新產圖。
  ⚠️ 行內樣式不吃 padding／border，左右內距只能靠 `&#160;` 撐；灰鈕要比按鈕本體深一階才看得見。
- **四頁照同一個骨架寫**：①這頁做什麼 ②還沒有資料時怎麼開始 ③設好之後接到哪一頁，
  細節分節寫短段（維護者裁示 2026-09-18）。全文圍繞「輪番設定訂規則 → 人員設定建名單
  → 產生月表組合」這條主線。
- ⚠️ **提示只有一種規格**（`note`，淡黃底＋ⓘ）：不再分資訊藍與警告黃兩種顏色，
  同一份說明裡顏色換來換去，讀的人分不出哪個比較重要。
- 功能、分頁、按鈕或畫面名稱改名時，須人工逐頁核對使用指引，並同步 `_BUTTON_ROLES`。
  內容測試只守四頁非空與固定索引，不以逐字比對代替人工核對。

### 產生月表

```
┌ 產生月表 [115▾]年 [10▾]月  已產生・自訂起始・2026-09-16 建立  [接續上月][自訂起始]  [匯出][刪除月表] ┐
┌ 預覽 ───────────────────────────────────────────────────────────────────────────────┐
│                         （整張 A3 月表的縮圖）                                         │
```

- **年月是兩個唯讀下拉**（民國年；今年前後各一年），預設帶下個月。
  ⚠️ 下拉一樣會吃滾輪，要掛 `ui_utils.installComboWheelGuard`
- **已產生的月份軟擋**：先確認覆蓋；過去月份確認框多一句
  「此為歷史勤休預定表，請確認是否要重新產生覆蓋原資料。」（刪除為「…是否要刪除資料。」）
  - 「自訂起始」覆蓋：配對彈窗預選該月模板並填入現有配對（`plan.prefill_from_month`，不往後推）
  - 「接續上月」覆蓋：用上月重新推一次蓋掉
  - ⚠️ 覆蓋是**刪舊與寫新同一個交易**，驗證都在刪之前做完；配對不完整時舊月表保留
- **匯出**：xlsx 與 pdf 一次產出，檔名 `115年10月輪番表.xlsx`／`.pdf`。
  預設存桌面（`QStandardPaths.DesktopLocation`，桌面被 OneDrive 接管也找得到）；確認框按鈕
  「匯出」「另存到其他位置…」「取消」，另存資料夾只用這一次、不記住
  （現場庫殘留的 `output_dir` 設定不讀也不刪）。同名檔先問覆蓋；檔案被開著（`PermissionError`）
  時提示「請關閉後再匯出」，不丟系統原文

#### 預覽是必要的，不是加分項

⚠️ **預覽不另寫畫表程式**：`ui_utils/sheet_preview.py` 直接呼叫 `export.pdf_writer.paint_sheet`，
畫到 A3 比例底圖（寬 2800px，約 169dpi）再縮放。預覽長什麼樣，印出來就長什麼樣。

- 預設左右填滿預覽區，上下超出出直向捲軸；⚠️ 算寬度要先扣直向捲軸的寬，否則連橫向捲軸都跑出來
- 最小＝整張塞進預覽區；上限＝底圖原始解析度（再大字只會糊）
- 滾輪以游標為中心縮放；動過滾輪後視窗縮放維持相對倍率，換月份回到預設
- 放大後按住左鍵拖曳移動（游標變手掌）；完整塞進時游標維持箭頭

#### 配對彈窗的內容

按「自訂起始」開獨立視窗做人員↔番號配對（不做成分頁）。

```
┌─ 設定配對：115 年 10 月 ─────────────────────────────────┐
│ 模板：[預設範例              ▾]                           │
│                                                           │
│ ┌─ 配對 ──────────────────────────┐ ┌─ 名單　在職 34 人 ┐ │
│ │ 群組   格位  代碼       人員     │ │ 共 34 格，尚有 1 格未分配│
│ │ 大輪番  1    01     [吳大雄  ▾] │ │ 尚有 1 人未分配：    │ │
│ │         5    05     [        ▾] │ │   王小明           │ │
│ │         6    06（輪休）[林小芳 ▾] │ │                    │ │
│ └──────────────────────────────────┘ └────────────────────┘ │
│                                                           │
│  [接續上月底填入] [全部清空]                  [取消] [確定] │
└───────────────────────────────────────────────────────────┘
```

- **模板下拉**：換模板＝配對清空重來，先確認
- **配對表**：所有群組排同一張表，群組名只寫在該組第一列，輪休格代碼欄標紅字「（輪休）」
- **名單**：全部在職人員；已配的變灰並標「大輪番 第 5 格」，不消失
- **點名單人名＝帶進目前那一格**（焦點所在的人員欄，整列淡藍底）；還沒點過格子時提示
  「請先點選左側要填入的格位」。名單不搶焦點；填入後把畫面捲回那一格
  - ⚠️ 偵測目前那一格要監聽 `focusChanged`，**不能掛在下拉 lineEdit 的 FocusIn**——可打字下拉的焦點落在 combo 本身
  - ⚠️ 名單項目**存人員編號**，不拿畫面姓名反查
  - ⚠️ **名單卡片固定寬度**（`pairing_dialog.ROSTER_W`）：寬度跟內容走的話每配一人配對表就被擠一下（PoliceDocSys LAY-7）
- ⚠️ 配對表套 `sort_table.TABLE_SS`，套了樣式表的表格**不畫 `setBackground` 的底色**，
  要標色得掛先畫底色的 delegate（`pairing_dialog._BackgroundDelegate`）

##### 人員欄：可打字篩選下拉，加兩條規矩

用公版 `ui_utils.widgets.makeFilterCombo`，只列在職人員。可打字 combo 在 PoliceDocSys 踩過一整族
（QTW-7／8／9／11／12，QTW-9 是選了 A 卻靜默回填成 B），公版已補強。
⚠️ 配對彈窗多一個連動——**選到已配在別格的人，那格會被清空**——所以兩條規矩不可拿掉：

1. **只有確實選中名字才寫進格位**：點候選／下拉項目，或完整打出姓名後按 Enter／離開欄位。
   打字過程中資料完全不動，否則打「李小」到一半就可能清掉別格的李小華。清空文字後按 Enter／離開＝改成未分配
2. **按確定前整張表檢查**：打了字卻沒選中名單的格位標紅、不准送出

⚠️ **不做拖曳**：表格＋自訂 delegate＋125% 縮放是高風險區。

##### 行為

- 選到已配過的人 → 原格直接改成未分配，不跳確認；⚠️ 被清空的格**短暫標黃底淡出**，讓視線跟得上
- 休的格位一樣要配人
- **沒配完不准按確定**並標出缺哪幾格。⚠️ `plan.create_plan` 寫入前會再檢查一次，真正的 gate 在資料層
- 被改過的格子標色

##### 填入按鈕

| 按鈕 | 用途 |
|---|---|
| `接續上月底填入` | 預填上月推到本月 1 日的番號。只沿用**名稱相同、每格代碼與休都一樣**的群組，對不上的整組留空並列出名稱；人用姓名對回在職名單，離職的那格留空 |
| `全部清空` | 重來，不確認 |

### 輪番設定

左邊模板清單（名稱＋建立日期；新增／複製一份／改名／刪除，刪除要確認並講明已產生月表不受影響），
右邊上半群組表、下半槽位方塊（`tabs/tab_rules.py`、`ui_utils/group_dialog.py`）。

- 提示條寫明「修改模板不會影響已經產生的月表」
- 沒有模板時整個編輯區不可操作，⚠️ 每個進入點各自檢查（反灰擋不住雙擊與拖拉）
- **群組表**只顯示（拖拉把手／序號／群組名稱／模式／型態／範圍／左側日期 有／無），新增與修改走彈窗；
  新群組預設名稱「群組1」「群組2」……、排在最後；順序可拖拉或打序號，按「儲存排序」才寫入
- **群組彈窗**：名稱、模式（輪番／固定番／空白欄，唯讀下拉）、範圍、左側顯示快速識別日期、
  反向排序（右往左）、代號位置（只有固定番顯示）、註記。
  按儲存就展開該組槽位；模式或範圍有變先確認
- **反向排序**：月表（預覽／Excel／PDF）上欄位左右顛倒，空白欄一樣適用；
  左側日期欄仍在群組左邊，配對彈窗格位順序不變
- **「檢查規則」**只做跨群組檢查（代碼衝突、整組都是休）
- **欄寬權重不給使用者調**，由模式決定：輪番 1.1、固定番 1.2、空白欄 1.2（`template.WEIGHT_BY_MODE`）
- **槽位方塊**：點一下切換休、雙擊改代碼。一般方塊上行番號、下行「N番」；輪休淡紅底寫「輪休」；
  自訂代碼淡黃底寫「原 xx」。卡片標題列「輪番群組的番號都由 1 起算」只作用在輪番類型
  （勾選時 `21-30` 第一格顯示 1番）；固定番一律依番號，空白欄不顯示 N番
- 畫面稱「輪休」，Excel／PDF 印「休」

### 人員設定

純名單：姓名、女警、在職／離職（軟刪除，**不提供刪除**）。
比照 PoliceDocSys 人員管理（`tabs/tab_personnel.py`、`ui_utils/member_dialog.py`、`lib/members.py`）：

- 欄位：拖拉把手／序號／姓名／女警（✓）／狀態；離職列整列淺灰
- 按鈕：新增、修改（雙擊列同效）、儲存排序
- ⚠️ 新增後**排在最前**；群組相反排在最後（版面由左到右接在後面）。刻意不同
- 調順序三條路徑：拖拉整列、點序號格打數字、彈窗「順序」欄；按「儲存排序」才寫入，
  新增／修改後保留未存順序，關閉程式時有未存排序會詢問
- ⚠️ 這個順序只是名單與配對下拉的顯示順序，不是輪番番號

### 功能維護

左側選單＋右側一次一張卡片（`ui_utils/side_nav.py` 的 `SideNavPage`；選單外觀在
`lib/theme.py` 的 `QFrame#sideNav`／`QPushButton#sideNavItem`）。
切換子頁時有未存修改 → 問「儲存／放棄修改／取消」，儲存失敗或取消留在原頁（`setLeaveGuard`）。
⚠️ 不用 `settings_panels._SettingsPanel`：它自帶寫死色碼的樣式表，外觀與卡片不一致。

- **月表標題**：單位名稱（`unit_name`），即時字數「N / 20」（逼近上限橘、到頂紅），
  「恢復預設」「儲存」（沒改反灰），空白不存。上限 20 字是量 PDF 標題欄定的（§8「最左標題欄」）
- **資料庫備份**：異地位置可留空＝不啟用；設了位置時顯示最近異地備份日期，
  超過 7 天、寫不進去、本次開機失敗都紅字；「立即備份」
- **壓縮資料庫**：一顆按鈕
- 切進分頁時重讀設定（有未存修改的面板不蓋掉）；切回產生月表時重讀單位名稱重畫預覽

## §5 測試

- 單元測試在 `tests/`，檔名 `test_*.py`；純邏輯（排班推算、代碼產生、版面模型）一律要有測試
- ⚠️ 125% 縮放下的切字、欄寬、裁切**只能上機看**，不要憑 `sizeHint`／`QFontMetrics` 數字回報修好
- ⚠️ 量字寬或截圖要 `QT_QPA_PLATFORM=windows`＋微軟正黑體 14pt；`offscreen` 會換替代字型，字寬不準
- **Excel 實際排版驗證**（openpyxl 看不到 Excel 怎麼排）：本機有 Excel 時用 COM `Workbooks.Open`＋
  `ExportAsFixedFormat` 轉 PDF，再用 `QtPdf.QPdfDocument.render` 畫成圖看。
  ⚠️ 轉檔前先刪舊 PDF，否則轉失敗會看到舊圖；打不開通常是檔案毀損（PITFALLS XLS-6）

## §6 初始資料（`db_seed.py`）

沒有匯入機制。第一次開啟時備好：

- **人員**：虛構姓名名單，其中三位女警（看得到紅字效果）
- **輪番設定**：一份照現行紙本區塊順序的模板

## §7 設定（`App_Settings`）

- 資料表 `App_Settings(key TEXT PRIMARY KEY, value TEXT NOT NULL)`，沿用 PoliceDocSys
- 設定畫面一律用卡片；「月表標題」行為照 PoliceDocSys `PrintTitlePanel`

| key | 用途 |
|---|---|
| `unit_name` | 單位名稱，印在標題。⚠️ 不可寫死任何單位名 |
| `backup_second_dir` | 異地備份位置；空字串＝不啟用 |
| `slot_number_from_one` | 槽位方塊「N番」：`1` 從 1 起算、`0` 依番號（預設）。只影響畫面 |

⚠️ **新增 key 時這張表要同步補一列。**

⚠️ **開機讀一次就快取的設定，存檔後要有人重新套用**（PoliceDocSys CFG-1：改了閒置登出時間，
沒重啟照樣一分鐘被登出）。面板不自己去碰其他元件，一律 emit 訊號讓上層處理。

## §8 列印版面（照現行紙本）

### ⚠️ 軸向：X 軸是人名，Y 軸是日期

**一位同仁一欄，一天一列。** 紙本是旋轉 90° 掃描的，掃描件上橫的東西在原始 Excel 裡是直的，
動版面前先確認軸向。回歸測試 `tests/test_layout_model.py::TestAxisOrientation`。

### 整體

- **A3 橫式單頁**，一個月一張
- 姓名與所有多字欄標題（含日期、星期）**直書**：一字一列疊下來，不是把橫書轉 90°
- ⚠️ **日期與星期欄在區塊之間重複**，是紙本既有設計不是冗餘；哪些區塊前面有由 `T_Group.header_before` 決定

### 區塊（由左而右）

```
標題 │ 日期 星期 │ 大輪番 20 │ 日期 星期 │ 固定番 8 │ 日期 星期 │
     早 中 晚 │ 幹部 6 │ 快打勤務 │ 日期 星期
```

| 區塊 | 模式 | 內容 | 格子 |
|---|---|---|---|
| 大輪番 | `rotate` | 20 人，每人一欄 | **程式填滿** |
| 固定番 | `fixed` | 21～28 共 8 人：姓名＋固定番號 | 留白供手填 |
| 同仁專案臨檢 | `blank` | 單欄，標題直書跨姓名列與代碼列 | 留白供手填 |
| 劃假 | `blank` | 早／中／晚三欄，上方一格跨三欄的註記 | 留白供手填 |
| 幹部 | `fixed` | A～F 六人：姓名＋代碼 | 留白供手填 |
| 快打勤務 | `blank` | 單欄，標題直書跨兩列 | 留白供手填 |

⚠️ 「同仁專案臨檢」與「劃假」是兩個不同區塊。
⚠️ **`blank` 模式的 `range_expr` 是逗號分隔的字面欄標題**（例 `早,中,晚`），不是範圍式。
⚠️ 後兩區的格子刻意空白，程式不要去填。

### 表頭合併（姓名列＋代碼列）

標題往下跨代碼列合併成一格：所有人名欄、日期、星期、空白欄的單欄標題。**只有劃假不合併**。

- ⚠️ 規則只寫在 `lib/layout_model.header_spans_code_row`，Excel 與 PDF（含預覽）共用。
  Excel 姓名列高度照這條**逐欄**算：合併的欄可用高度多一列
- **固定番／幹部**：代號與姓名同一格，姓名一字一行、代號橫排佔一行，上下由「代號位置」決定。
  代號一律黑字
- Excel：⚠️ 不能用 textRotation 直排（會把「21」拆成上下兩字），改**換行疊字＋RichText 分色**；
  換行格不能配縮小字型，字級自己算（`_digit_size`）。
  ⚠️ RichText 不可有「只含換行」的一段，存檔會變空字串、Excel 判定毀損打不開（PITFALLS XLS-6）
- PDF：代號那行只佔筆畫高度再多留 `CODE_EXTRA_GAP`（英文字母比中文矮）

### 直書裡的數字與空白

- **最左標題欄**：中文一字一行，連續數字／英文（115、10）併成一行橫排
  （`layout_model.vertical_pieces`，Excel 與 PDF 共用），空白略過
- **日期、星期**：兩字中間兩個全形空白（`HEADER_GAP`），空白只佔一般字高的 `GAP_SCALE`（0.5）

### 數字與英文的字型

純半形字串（番號、日期、代號、標題的 115）用 **Tahoma**，中文標楷體；中英混排的註記不換。
PDF `pdf_writer.DIGIT_FAMILY`、Excel `xlsx_writer.DIGIT_FONT_NAME`。
⚠️ Tahoma 粗體數字比半個字寬估算寬，Excel 換行格預留 `DIGIT_EM_SLACK`（1.45）倍，否則「27」會折成兩行。

### 字級（不切字）

格子裡的字儘量佔滿，以不切字為主；Excel 與 PDF 分開設定。範圍是每天的格子、代碼列與直書姓名；
劃假上方註記不動；最左標題欄見下一節。

- **PDF**（`pdf_writer.FILL_RATIO` 0.70；輪番格番號數字 `BODY_FILL_RATIO` 0.60）：
  畫出實際會出現的字、掃描**筆畫範圍**，放大到最寬最高的字佔該比例。左右以字寬置中、上下以筆畫置中；
  直書姓名整串共用一條上下中線。同類格子共用字級（取最窄的欄）；直書姓名以欄寬為主，
  名字長到放不下時只縮那一格。
  ⚠️ 字一律轉外框路徑再填色（`_draw_text`），不用 drawText（PITFALLS QT-6）
  ⚠️ 不能用 `QFontMetricsF.tightBoundingRect` 量：標楷體中文字它回傳整個字框（PITFALLS QT-5）
- **Excel**（`xlsx_writer.XLSX_FILL` 0.85）：openpyxl 量不到字寬，依點數估（中文一字＝一個字級寬、
  半形＝半個；行高＝字級 × `LINE_RATIO`），並打開「縮小字型以適合欄寬」兜底。
  姓名列高度以 `NAME_ROW_CHARS`（4）字為準，更長的只縮自己那格。
  ⚠️ **日期欄**半形寬度照 `DATE_EM_SLACK`（1.2）估：照半個字寬估，Excel 只縮兩位數，1～9 會大一截（PITFALLS XLS-8）

### 最左標題欄

中文與數字分開定字級，標題欄權重 1.4：

- 中文字寬佔欄寬：PDF `TITLE_FILL` 0.80；Excel `XLSX_FILL`，上限 `TITLE_MAX_SIZE` 32pt
- 「115」「10」另外縮到塞得進欄寬（PDF `TITLE_DIGIT_FILL` 0.85；Excel `_digit_size`），所以數字比中文小
- PDF 標題**不加粗**、行高 `TITLE_LINE` 1.25（加粗與較小行高會擠成一團）；Excel 仍粗體
- 排不下欄高時中文與數字一起縮（PDF 留 4% 餘裕；Excel 照 `VERTICAL_LINE_RATIO` 估）
- 字級計算在兩個 renderer 各一支 `title_sizes`（純計算），測試驗「中文比數字大」「不超出欄高」

### 區塊註記

劃假三欄上方的說明存在 `T_Group.note`、隨快照一起複製（內容與輪番規則綁在一起，換單位就不同）：

```
晚班:(1-5、16)
早班:(8-12、15)
中班(17.18)
限填1人
```

**一律純文字、黑字**：一行一筆，空行略過（`lib/layout_model.py:parse_note`）；xlsx 寫多行字串、PDF 逐行畫。

### 欄寬權重

| 區塊 | 權重 | 34 人時實際寬度 |
|---|---:|---:|
| 標題 | 1.4 | 10.4 mm |
| 日期／星期 | 1.0 | 7.4 mm |
| 大輪番 | 1.1 | 8.2 mm |
| 固定番／同仁專案臨檢／幹部／快打勤務 | 1.2 | 8.9 mm |
| 劃假 | 1.4 | 10.4 mm |

⚠️ 權重定義在 `lib/layout_model.column_weight`，**兩個 renderer 共用**，各寫一份 Excel 就會跟 PDF 不一樣寬。

### ⚠️ 版面隨人數自適應

欄寬與列高一律由可列印區依權重分配，不得寫死：

- 人多 → 欄變窄（下限 3.0，約 19.5pt）；人少 → 欄變寬（上限 10.0）
- 28 天的月份 → 列變高，一樣填滿整頁
- 一頁容得下約 **58 欄**（`xlsx_writer.max_columns_per_page()`）；超過才交給 `fitToPage` 整張縮小

### 顏色

- **休印紅色**
- **星期六、日**那幾列的日期與星期印紅色
- **女警姓名**印紅色（番號不變色）
- 其餘黑色

## §9 已排除的需求

以下經維護者裁示**不做**，不要再提案加回來：

- **同一個人同時在兩個群組** → 不允許，配對時一人只能配一格
- **權限系統** → 只有承辦人一人使用
- **班別註記**（早／中／晚班對應哪些番號）→ 紙本右下角是人工備註，不由程式產生
- **舊資料匯入** → 不做
- **還原鈕、回收桶、修改前快照、匯出資料夾設定** → 不做

## §10 打包

```
python -m PyInstaller --clean --noconfirm PoliceRotaSys.spec
```

輸出 `dist/PoliceRotaSys.exe`（onefile，約 25.8MB）。資料庫、`error.log`、`backups/` 建在 exe 旁。

### PDF 快速指引與 release 附件

快速指引的操作文案集中在 `ui_utils/help_content.py`，`tools/gen_quickstart.py` 只負責排版。
工具使用 PySide6 的 `QPdfWriter` 產生 A4 直式 300 dpi PDF，完成後會確認檔案非空，
再以開發環境的 `QtPdf.QPdfDocument` 載入並驗證正好兩頁；任何檢查失敗都會以非零結束碼離開。
`QtPdf` 只用於此開發工具的產物驗證，不得移入產品 runtime 或打包相依。

⚠️ **版面一律照量到的字高排，不寫死 y 位移與框高**：頁首的大標在 300 dpi 下上下伸出得比
想像多，寫死框高會把字裁掉並壓到副標；卡片高度也要照內容長，平均分配整頁會讓每張卡片
中間空一大塊、提示條被壓在底部。剩餘空間平均分給卡片之間的間距（上限 120），內容真的
放不下才報錯。

一般重建使用預設輸出路徑（PDF 受 `.gitignore` 排除，不入庫）：

```powershell
python tools/gen_quickstart.py
```

發版時把 exe 與 PDF 上傳到同一個 release。⚠️ **附件檔名固定為 `PoliceRotaSys.exe` 與
`Quick_Start.pdf`，不加版號**（維護者裁示；版號看 release 標題即可）：

```powershell
$version = python -c "from lib.version import __version__; print(__version__)"
python tools/gen_quickstart.py dist/Quick_Start.pdf
gh release upload "v$version" dist/PoliceRotaSys.exe dist/Quick_Start.pdf
```

上傳前仍須目視兩頁，確認沒有缺字、文字溢出或不自然分頁。產生器成功訊息會列出實際字型、
檔案大小與頁數；若沒有微軟正黑體，會依工具內的中文字型清單選擇能顯示全部文字的替代字型。

程式圖示 `res/buttons/police_badge.ico`（exe）／`.svg`（視窗）與啟動畫面 `res/buttons/banner.png`
（`ui_utils/loading_screen.py`）由 spec 的 `datas` 帶進去，經 `lib/resource_path.py` 找路徑（打包後在 `sys._MEIPASS`）。
⚠️ 圖檔也佔 exe 大小：banner 畫面只顯示 700×279，存 1050 寬即可；ico 的 256 層要存 PNG 壓縮。

⚠️ **spec 入庫的理由**：①命令列砍不掉 hook 以 binary 身分收進來的 DLL 與語系檔，瘦身只能在 spec 裡過濾；
②設定不入庫就無法重現舊版本的 build。`.gitignore` 以 `!PoliceRotaSys.spec` 放行。

排除清單**唯一來源是 `tools/pyi_prune.py`**（spec 的 `excludes` 也從它取）。已排除：軟體 OpenGL、
QML／Quick、PDF 閱讀模組、網路、多餘圖片格式（含 `qjpeg`）與語系檔、備用顯示外掛、打包機 PATH 上誤撿的
Git OpenSSL；標準庫的 `ssl`／`_hashlib`（程式不連網）、`lzma`／`bz2`（xlsx 只用 deflate）、
`defusedxml`（本程式只寫檔，它會拉進 xmlrpc、pydoc）。
⚠️ 語系檔只保留 `zh_TW`：開機時 `installChineseTranslator`（`ui_utils/widgets.py`）載入 `qtbase_zh_TW.qm`，
捲軸／輸入框右鍵選單與標準按鈕才會是中文；砍掉它打包版會退回英文，原始碼跑卻正常。

⚠️ 評估過**不可砍**：`qmodernwindowsstyle`（底層外觀會變）、`unicodedata`（`traceback` 會用，寫錯誤紀錄時
再出錯就查不到）、三份重複的 VC 執行階段 DLL（PySide6／shiboken 各自從自己的資料夾載入）、
UPX（Qt DLL 壓縮後易出問題、易被防毒誤判）。

⚠️ 打包版沒有 `qoffscreen`：exe 設了 `QT_QPA_PLATFORM=offscreen` 會直接崩潰（0xC0000409），驗證一律用正常顯示。

⚠️ 本專案**不提供 `loadUi`**：`QtUiTools` 一 import 就連帶要求 Qt6UiTools → Qt6OpenGLWidgets → Qt6OpenGL
整串 DLL。公文系統有 UiTools，**它的排除清單不可原樣抄回**。

⚠️ **改排除清單後的檢查**：build 成功、原始碼跑得動、測試全過都**不代表**打包版能用（PITFALLS PKG 組）。
至少要：開啟 exe；產一個月表看預覽；匯出一次 Excel 與 PDF；確認勾選框與下拉箭頭（SVG 圖示）有畫出來；
確認啟動畫面與視窗圖示有出現。沒辦法手點匯出時，寫一支小程式直接呼叫匯出，用同一份
`EXCLUDES`／`prune` 另外打包執行（探針不入庫）。

## §11 README 公開截圖

README 的七張正式截圖由兩支入庫工具重建，不得使用根目錄 `dbfile.db` 或任何現場資料。
先在專案根目錄建立候選資料庫，再於 Windows 的 125% 顯示縮放下擷取：

```powershell
python tools/seed_screenshot_data.py .tmp/readme-screenshot.db --force
$env:QT_QPA_PLATFORM = "windows"
python tools/capture_screenshots.py --db .tmp/readme-screenshot.db --output docs/img --force
```

`seed_screenshot_data.py` 沿用正式 schema 與種子流程，加入匿名單位、一名虛構離職人員及
執行當時相鄰兩個月份的完整月表。它先在輸出檔同一資料夾完成暫存資料庫，再以原子替換
寫入；既有輸出必須加 `--force`，且即使有 `--force` 也永遠拒絕根目錄 `dbfile.db`。
完成後會把匿名單位、全部設定（雜湊欄本身除外）、人員狀態／性別／順序、模板、群組、
槽位及兩個月表快照以固定排序序列化，儲存 SHA-256 內容指紋。候選資料庫只是本機素材，
不得提交。

`capture_screenshots.py` 只接受專用標記與內容指紋都吻合的候選資料庫；建庫後若修改單位、
人員、模板、格位或月表快照就會拒絕截圖。工具固定使用 Windows Qt、微軟正黑體 14pt、
產品全域樣式與 `widget.grab()`。七個檔名及畫面狀態集中定義於工具內；第 6 張固定切到
「資料庫備份」子頁。既有圖片同樣必須加 `--force` 才能覆寫，禁止改用 `offscreen` 產正式圖片。

每次重建後都要逐張目視 `docs/img/`：確認畫面不是空白或錯誤狀態、文字無截斷、配對與預覽
確實有內容，且只出現虛構姓名與「○○分局○○派出所」。入庫或 push 前另依規定執行
`python -m unittest tests.test_no_pii`；自動檢查不能取代人工目視。
