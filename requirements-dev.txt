# 開發／測試／打包相依。不進 exe，不受 requirements.txt 的封閉清單限制。
#
# 安裝：
#   pip install -r requirements-dev.txt
#
# 跑測試（⚠️ `-t .` 不可省）：
#   python -m unittest discover -s tests -t .
#   python -m unittest tests.test_no_pii          ← push 前必跑
#
# ⚠️ Linux／無 GUI 環境跑 PDF 測試要多兩件事，Windows 不必：
#   1. 設 QT_QPA_PLATFORM=offscreen
#   2. 裝 Qt 的系統函式庫（PySide6 的 wheel 不含）：
#      apt-get install libegl1 libgl1 libxkbcommon0 libdbus-1-3 libfontconfig1
#   見 PITFALLS.md QT-1。

-r requirements.txt

# 測試
pytest==9.1.1
pytest-qt==4.5.0

# 打包（PyInstaller --onefile，見 DEVELOPER）
pyinstaller==6.16.0
