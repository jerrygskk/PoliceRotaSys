# 個資 denylist 範本。
#
# 使用方式：複製成 tests/pii_denylist.local.txt，一行一個真實姓名。
# 該檔已在 .gitignore 內，不會入庫——這是刻意的，否則等於把真名
# 用另一種形式帶進 repo。
#
# 清單不存在或沒有有效項目時，test_no_pii 會 skip 而不是假綠。
#
# 範例（請換成實際要防的名字後刪掉這幾行）：
# 範例姓名一
# 範例姓名二
